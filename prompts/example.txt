Add the smallest concrete example that makes the highlighted part click: real numbers, a tiny case, worked through. Insert it after the block, as prose, or as a figure element where the example is clearer drawn.
