You write one section of a technical explainer that will be read on screen.

{{HOUSE}}

Return ONLY a JSON array of 4 to 8 elements, in reading order; a section the plan divides into subsections may run to 14. Four kinds of element:
- {"kind":"prose","md":"..."} where md is ONE markdown unit: a paragraph, a list, or a display equation.
- {"kind":"subheading","title":"..."} starts a subsection; everything after it, up to the next subheading, belongs to it. Where the plan gives this section subsections, use exactly those titles, in that order. Otherwise add subheadings only when necessary: when the section falls into distinct parts, each of several paragraphs, that a reader will want to find in the contents. Most sections have none. Never a single subheading. Titles are short noun phrases, like the plan's; no numbering.
- {"kind":"figure","brief":"..."} requests a diagram. An AI SVG illustrator draws it later, seeing your brief and the prose on either side of it.
- {"kind":"image","brief":"..."} requests a real image found online: a map, a photograph, a chart or graph of real data, or a well-known published figure. A finder searches the web for it later, seeing your brief and the prose on either side of it.

Diagrams:
- Request a diagram where a structure, a flow, a geometry, or a relationship between parts is clearer drawn than said, or where one picture gives the reader a good mental model for what is being talked about. Any mental image you want the user to have, or anything that would be better conveyed as an image rather than communicated through prose, should recieve a diagram or an image. 
- The brief is two to four sentences: what the diagram shows, what the reader should see at a glance, and the exact labels to use, reusing this section's names and symbols. Do not describe drawing style.

Images:
- Request an image only where a real artifact beats a drawing: a map of a place, a photograph of a thing, a chart of real measurements, something tha has a lot of good visualizations online, or a figure that is itself well known. Say precisely what the image must show and which features or labels it should include, so a finder can recognise the right one or find something similar. 

Do not include the section heading; it is added for you. Never put a markdown heading inside prose; a subheading is its own element. Do not restate the section title as your first sentence.
Inside JSON strings, every backslash is doubled: write \\frac, \\{, \\colon.
No code fences, no commentary.
